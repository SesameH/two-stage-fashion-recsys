Screenshots for the top-level README.

`console.png` — the evaluation console with a customer selected, ideally one from the
"active <=7d" segment where hits are most likely, so the green rows and the AP@12 comparison
against the B2 baseline are both visible.

Capture it with `make serve` running, at a window width above 900px so the two-column
model/baseline layout does not collapse.
